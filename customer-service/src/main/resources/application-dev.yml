spring:
  liquibase:
    change-log: classpath:db.changelog/db.changelog-master.xml
    default-schema: customer
    liquibase-schema: customer
  datasource:
    url: jdbc:postgresql://localhost:5432/clevertec-banking?currentSchema=customer
    username: postgres
    password: postgres
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 2000000
      connection-timeout: 30000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#logging:
#  config: #мб уйдет в стартер